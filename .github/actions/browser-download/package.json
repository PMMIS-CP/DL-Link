{
  "name": "browser-download-action",
  "version": "1.0.0",
  "description": "GitHub Action: Puppeteer-based browser navigator for extracting direct download links",
  "private": true,
  "scripts": {
    "install-deps": "cd src && npm install --production"
  }
}